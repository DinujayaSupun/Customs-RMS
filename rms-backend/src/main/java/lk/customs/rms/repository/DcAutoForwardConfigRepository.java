package lk.customs.rms.repository;

import lk.customs.rms.entity.DcAutoForwardConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DcAutoForwardConfigRepository extends JpaRepository<DcAutoForwardConfig, Long> {
}
