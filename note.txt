.\mvnw clean spring-boot:run
mvn spring-boot:run


# romved this dependency as security is not needed now
        <dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>